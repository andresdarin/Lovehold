'use client'

import AuthLoginExperience from '@/features/auth/AuthLoginExperience'

/**
 * Vista de Login de Finnic con micro intro animada (~950ms),
 * layout monolítico ligero y estética premium old-money.
 */
export default function LoginPage() {
  return <AuthLoginExperience />
}

